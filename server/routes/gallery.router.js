const { Router } = require('express')
const router = Router()

// PUT /gallery/like/:id
router.put('/like/:id', (req, res) => {
  // code here
})

// GET /gallery
router.get('/', (req, res) => {
  // code here
})

module.exports = router
